<%= simple_form_for [current_user, @image],html: { class: "col-md-6 col-md-offset-3"} do |f| %>

<%= f.input :caption %>
<%= f.input :public %>
<%= f.input :image, as: :file %>

<%= f.button :submit %>

<% end %>